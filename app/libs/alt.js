// import the Alt library into the Alt namespace.
import Alt from 'alt';

// Uncomment this line to use the chrome debug tool for alt.
//import chromeDebug from 'alt-utils/lib/chromeDebug';

// create a single variable for an instance of Alt
const alt = new Alt();

// always return the same instance of alt
export default alt;